
---

## 📘 `day2.md`

```markdown
# Day 2 – UFW & Fail2Ban Basics

## 🔥 What is UFW?

UFW (Uncomplicated Firewall) helps block or allow traffic by setting rules.

Common UFW Commands:

```bash
sudo ufw enable
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow OpenSSH
sudo ufw status verbose
